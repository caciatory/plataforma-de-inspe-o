# Roadmap — Check Auto v1.0

Atualizado: 2026-08-14. Baseado em `docs/especificacao-tecnica-v1.md` §5 (9 fases) e no estado real do código (branches, migrations) nesta data.

Cada passo abaixo é uma unidade fechada: você cola o prompt sugerido, o processo documentado em `docs/PROCESSO.md` (`brainstorming` → `writing-plans` → `subagent-driven-development`) roda até o fim, e o passo só é considerado pronto quando passar pelas 3 skills de fechamento (seção final deste doc). Só então vá para o próximo prompt da lista.

**Regra deste documento:** ao final de cada fase concluída, este roadmap é atualizado com um resumo simples do que foi feito (seção "Progresso" abaixo) e, se algo descoberto durante a task justificar, uma nova fase é sugerida/inserida na lista.

## Progresso

- **Passo 0.1 — concluído (2026-07-19).** `worktree-fase1a-dados-basicos` (RF-01 login, RF-02–06 dados básicos) revisado via `requesting-code-review` (sem issues Critical; 1 Important — erros de RPC/query descartados sem log — corrigido antes do merge), mesclado em `main` via `finishing-a-development-branch`, worktree removido, branch local apagado. `main` agora tem código de app pela primeira vez. **Descoberta durante a task:** arquivos `._*` (AppleDouble, gerados pelo Finder/macOS neste volume externo KINGSTON) são pegos pelo glob de teste do Vitest e quebram a suíte com erros de parse falsos — não é regressão do merge, é um problema de ambiente que vai se repetir em toda fase futura que rodar `npm test`. Ver Passo 0.4 (novo, sugerido) abaixo.
- **Passo 0.2 — concluído (2026-07-19).** `worktree-fase2-preenchimento-item` **não era redundante como o roadmap supunha** — suas migrations (00011–00014) já estavam em `main`, mas os 4 arquivos de teste SQL e o doc do plano (`docs/superpowers/plans/2026-07-11-fase2-preenchimento-item.md`) nunca tinham sido trazidos. Copiados para `main`. **Descoberta durante a task:** os 4 testes falhavam contra a DB real (`duplicate key value violates unique constraint checklist_group_templates_ordem_key`) — os fixtures usavam `ordem = 1` pra um grupo sintético, e esse valor colidiu com o grupo real "Identificação" semeado pela migration `00016`. Corrigido bumpando `ordem` pra 901–904, seguindo a convenção que o teste da migration `00015` já usa (`ordem >= 900` pra fixtures). Todos os 23 asserts passam contra a DB real após a correção. Branch e worktree encerrados.
- **Passo 0.3 — concluído (2026-07-19).** `README.md` e `docs/database-schema-v1.md` sincronizados com o estado real: código de app existe (Fase 1a), RLS está implementada nas 12 tabelas (não 11), colunas `ativo`/`observacoes` e as invariantes de `paint_measurements`/`checklist_item_responses` das migrations 00011–00016 documentadas, planos que existiam mas não estavam listados (RLS, fase1a, fase2, checklist-seed) adicionados à tabela de docs do README.
- **Passo 0.4 — concluído (2026-07-19).** `**/._*` adicionado ao `exclude` do `vitest.config.ts`. `npm test` volta a rodar limpo: 6 arquivos, 22 testes, zero suítes fantasma.
- **Fase 1 (restante) — concluída e mesclada (2026-07-20).** RF-09 a RF-12 (navegação do checklist: painel de grupo, indicadores de progresso, redirect pro primeiro grupo ativo, link do resumo da inspeção). Verificado ponta a ponta no navegador com conta de teste (`teste@checkauto.pt`). Revisada via `requesting-code-review` (0 Critical, 0 Important) e `ponytail-review` (nenhum achado — "Lean already"). 37/37 testes passando. Mesclada via PR #3.
- **Validade da inspeção — concluída e mesclada (2026-07-20).** Feature ad-hoc (não é uma das 9 fases originais; é trabalho preparatório pra Fase 5 — RF-57–61, lista de inspeções do admin) desenhada via `brainstorming` → `writing-plans` → `subagent-driven-development`: coluna `vehicle_data.quilometragem`, função pura `computeInspectionValidity` (validade de 6 meses + limite de 100km), campo obrigatório no formulário, selo de validade na página de detalhe. Revisada via `requesting-code-review` — achou e corrigiu 1 bug Important (overflow de `setMonth` em fim de mês, ex. 31/ago virava 03/mar em vez de 28/fev) e 1 Minor (doc de schema desatualizada). 33/33 testes passando. Mesclada via PR #2. Doc: `docs/superpowers/specs/2026-07-19-validade-inspecao-design.md`.
- **Ambiente — projeto movido pro Desktop (2026-07-20).** Disco externo KINGSTON caiu/remontou repetidamente durante a sessão (uma vez no meio de um `git worktree remove`, corrompendo um worktree). Cópia de trabalho movida pra `~/Desktop/bild app` (SSD interno); KINGSTON mantido como backup, não é mais o diretório ativo.
- **Fase 2 (UI) — implementada via subagent-driven-development, PR aberta (2026-07-20).** RF-13 a RF-22: bucket de Storage pra fotos (`fotos-inspecao`, público) + policy de RLS, RPC `save_paint_measurement` (upsert atômico + classificação derivada de `resultado_calculado`), formulário de classificação (ótimo/médio/ruim/NF + confirmação de NF + observação + fotos), formulário de medição (3-5 pontos numéricos), navegação "salvar e próximo" via `findNextItemId`. 8 tasks, cada uma com review própria; 1 fix durante a execução (policy de Storage sem bypass de admin, RF-35, aprovado antes de corrigir) e 1 fix pós-review-final (item de medição sem campo de observação, contradição com o design §3, corrigido e re-revisado limpo). 65/65 testes passando, `tsc`/build limpos, verificado ponta a ponta no navegador (login → classificar → foto → RF-16 bloqueio/liberação → confirmação NF → medição → conferido direto no banco). PR #4 mesclada em 2026-07-20. Design: `docs/superpowers/specs/2026-07-20-preenchimento-item-design.md`, plano: `docs/superpowers/plans/2026-07-20-preenchimento-item.md`.
- **Fase 2.5 — "Aplicar aos demais" implementada via subagent-driven-development, revisada (2026-07-21).** Resolve a pendência do §6 da especificação técnica. 9 tasks: coluna `grupo_replicacao` + CHECK constraint (só itens `tipo='padrao'`), curadoria de 101 itens em 37 clusters via script Python gerador de migration + CSV (`scripts/generate_grupo_replicacao_seed.py`), RPC `apply_classificacao_batch` (upsert atômico multi-linha, respeita o trigger RF-16 — um item "ruim" sem foto no lote reverte o lote inteiro), função pura `deriveSiblingRows` (itens-irmãos + default de checkbox: marcado só se `pendente`), `PhotoManager` adaptado pra múltiplas instâncias na mesma página, `applyClassificacaoBatchAction` (Server Action com validação client-side antes do RPC), `BatchApplyPanel` (painel de revisão em lote, bloqueio client-side de "ruim" sem foto nomeando o item), wiring na página de item existente (sem rota nova). Cada task revisada individualmente; revisão final de toda a branch encontrou 2 achados Importantes (nota de item-irmão já respondido mostrava status genérico em vez da classificação real; ausência de teste cobrindo o invariante "irmãos nunca herdam foto do item de origem") — ambos corrigidos e re-revisados limpo. 85/85 testes passando, `tsc`/build limpos, verificado ponta a ponta no navegador com conta técnico de teste (agora `teste1@checkauto.pt` — a conta original `teste@checkauto.pt` perdeu a senha). Migrations 00024–00026. Design: `docs/superpowers/specs/2026-07-21-aplicar-aos-demais-design.md`, plano: `docs/superpowers/plans/2026-07-21-aplicar-aos-demais.md`. Mesclada em `main` via `finishing-a-development-branch` (merge local, fast-forward) e enviada pro GitHub em 2026-07-22.
- **Sistema de design definido (2026-07-22).** O app não tinha nenhum estilo visual (`app/globals.css` era um reset de 2 linhas, HTML semântico puro em todas as telas). Usando a skill `impeccable`, definidos `PRODUCT.md` (registro "product", personas técnico/admin/cliente, princípios de design) e `DESIGN.md` (paleta OKLCH completa ancorada na marca **Check Auto** — verde `#11C685` estendido numa rampa de 10 tons, cores semânticas novas pra classificação ótimo/médio/ruim/NF, tipografia Space Grotesk + Inter, decisão de preto só nos pontos de identidade — header/nav/CTAs — não a interface inteira). **Descoberta durante a task:** a fonte de marca oficial (Codec Pro) só está disponível pro projeto via um link de uso pessoal (1001fonts, licença CC BY-NC) — não pode ir pra produção; Space Grotesk usada como substituta até haver licença comercial da Zetafonts (fundição original). **Descoberta 2:** o produto perdeu o domínio `inspecta.*` e o nome real da marca agora é **Check Auto** — o código/docs ainda dizem "Inspecta" em todo lugar (ver Fase 2.6 abaixo).
- **Fase 2.6 — Rename Inspecta → Check Auto concluída (2026-07-22).** Desenhada via `brainstorming` → `writing-plans` → `subagent-driven-development` (task única). Trocado o nome de marca em 7 arquivos "vivos": `README.md`, `CLAUDE.md`, `docs/ROADMAP.md`, `docs/database-schema-v1.md`, `package.json`, título da aba (`app/layout.tsx`) e título da tela de login (`app/login/page.tsx`). Documentos históricos datados (`docs/superpowers/specs/*`, `docs/superpowers/plans/*`), o relatório `journey-into-inspecta.md`, migrations já aplicadas e o nome do arquivo `checklist-inspecta-v5.csv` ficaram intocados por decisão explícita — são registro histórico, não branding atual. Nome do repositório GitHub não muda (já era descritivo em português, não continha "Inspecta"). 85/85 testes passando, `tsc` limpo, verificado no navegador (título da aba e heading do login mostram "Check Auto"). Mesclada em `main` via `finishing-a-development-branch`. Design: `docs/superpowers/specs/2026-07-22-rename-check-auto-design.md`, plano: `docs/superpowers/plans/2026-07-22-rename-check-auto.md`.
- **Fase 2.7 — Design system aplicado às telas existentes, concluída (2026-07-22).** Desenhada e implementada diretamente via skill `impeccable` (sem o ciclo `brainstorming`→`writing-plans`→`subagent-driven-development` — o próprio prompt da fase já apontava pra esse atalho, e o design já estava definido em `DESIGN.md`). `app/globals.css` reescrito de um reset de 2 linhas pros tokens completos do `DESIGN.md` (paleta OKLCH, tipografia, spacing/radius) mais primitivas CSS compartilhadas (`.btn`/`.input`/`.field`/`.panel`/etc — CSS global em vez de biblioteca de componentes, dado o tamanho pequeno do app). Fontes Space Grotesk/Inter via `next/font/google`. Todas as 8 telas com markup (login, dados básicos, resumo da inspeção, shell do checklist, grupo, item classificação/medição, aplicar aos demais) e o componente compartilhado `PhotoManager` restilizados; as 2 rotas restantes (`app/page.tsx`, `checklist/page.tsx`) são só redirect sem markup, confirmado sem HTML a estilizar. **Descoberta durante a task:** um worktree criado via `EnterWorktree` foi parar no disco KINGSTON (corrompido/desativado) em vez do Desktop — detectado antes de qualquer commit, removido, recriado manualmente em `~/Desktop/bild app/.claude/worktrees/`; ver `project_moved_to_desktop` na memória pro detalhe do gotcha (ferramentas nativas de worktree seguem o cwd de lançamento da sessão, não o `cd` mais recente). Revisão via `requesting-code-review` encontrou 2 Importantes (uma tela — `inspections/new/page.tsx` — ficou sem estilo por engano; alvo de toque de 44px inconsistente no nav do checklist) e 1 achado de contraste (badges de status do nav usando cor `.500` sólida em vez do padrão `.100`/`.600-800`) — todos corrigidos e re-verificados. `ponytail-review` removeu tokens/classes mortos (paleta dark mode nunca usada, `--shadow-float`, `.page-wide`, `.input-error`, `.badge-*` não utilizado). 85/85 testes passando, `tsc`/build limpos. Verificação no navegador: tela de login real (Playwright, sem senha de teste disponível — usuário optou por pular login e verificar as demais telas via harness estático isolado usando o `globals.css` real). Mesclada em `main` via `finishing-a-development-branch` (merge local, fast-forward) e enviada pro GitHub em 2026-07-22.
- **Fase 2.7 rejeitada pelo usuário (2026-07-22).** Ao ver o resultado rodando no navegador, o usuário classificou o visual como "bem ruim" e pediu um redesign de verdade — não só cores/tipografia aplicadas ao layout de formulário existente, mas um dashboard de verdade (sidebar de navegação por seções/subseções, tabela editável inline por subseção, tabs de dados do veículo), inspirado em 10 screenshots de referência de outro sistema de inspeção veicular. Decisões fechadas via `brainstorming`: adota estrutura + visual da referência (não só cores), mantém a paleta verde Check Auto (sem roxo), tabela-por-subseção com rotas normais do Next.js (não SPA). Durante o brainstorming, o usuário entregou o documento real do checklist (`checklist_inspecta_v7.md`, 360 itens/13 categorias) — revelou que o schema atual (2 tipos fixos: classificação universal + medição só de tinta) não suporta os ~30 tipos de resposta reais do documento. Escopo dividido em 4 peças sequenciais, cada uma com ciclo completo `brainstorming → writing-plans → subagent-driven-development`: **1a** (schema genérico de tipos de resposta), **1b** (adaptar a camada de app ao schema novo, mantendo a UI de hoje), **2** (re-seed do checklist real de 360 itens), **3** (o redesign visual/dashboard em si). Ver Fase 2.8 abaixo.
- **Fase 2.8, Peça 1a — Modelo genérico de tipos de resposta, schema concluído (2026-07-23).** 8 tasks via `subagent-driven-development`, cada uma com review própria: catálogo de opções compartilhado (`conjuntos_opcao`/`opcoes`, substitui o enum fixo `item_classificacao`), enum `item_template_tipo` generalizado (`escolha`/`texto`/`data`/`medicao`, era só `padrao`/`medicao`), colunas de faixa de referência configuráveis em `checklist_item_templates` (generaliza os limiares fixos de tinta da migration `00012` pra qualquer unidade de medição), `checklist_item_responses` generalizada (`opcao_id`/`resposta_texto`/`resposta_data` no lugar de `classificacao`), `status` e o resultado de medição viraram *views* (`checklist_item_status`, `medicoes_resultado` — não dá pra ser coluna gerada quando depende de outra tabela), RF-16 (foto obrigatória) generalizado pra checar `opcoes.exige_foto` ou resultado de medição `critico`, RPCs `save_medicao`/`apply_opcoes_batch` substituindo as versões só-tinta/só-classificação. **3 tasks encontraram bugs reais de sequenciamento de dependência no próprio plano** (coluna gerada precisa cair antes da coluna-fonte; gatilho com cláusula `OF coluna` bloqueia o drop da coluna; colisão de nome de variável PL/pgSQL entre `RETURNS TABLE` e `ON CONFLICT`) — todos verificados de forma independente pelos revisores (reprodução em sandbox, não só aceitos por confiança. Revisão final encontrou 1 Crítico real (2 views novas sem `security_invoker`, reintroduzindo o mesmo vazamento de RLS entre técnicos que a migration `00007` já corrigiu uma vez) — corrigido e re-verificado com prova de RLS entre dois técnicos de teste. Limpeza de testes SQL obsoletos (8 arquivos referenciavam `'padrao'`/`paint_measurements`/`classificacao`, agora removidos ou portados pro schema novo). **Só há dados de teste no banco (autorizado a apagar pelo usuário) — migração é clean-slate, sem conversão de dados reais.** 12 migrations (`00027`–`00036`). Design: `docs/superpowers/specs/2026-07-22-modelo-generico-tipos-resposta-design.md`, plano: `docs/superpowers/plans/2026-07-22-modelo-generico-tipos-resposta-1a-schema.md`.
- **Fase 2.8, Peça 1b — Adaptar a camada de app ao schema genérico, concluída (2026-07-23).** 9 tasks via `subagent-driven-development`, cada uma com review própria: `lib/checklist/progress.ts`/`siblings.ts` migrados pra `respondido: boolean`/`opcao_id`/`opcao_label` (troca a `status`/`classificacao` antigos), `actions.ts` reescrito (`saveEscolhaAction`, `saveMeasurementAction` → RPC `save_medicao`, `applyOpcoesBatchAction` → RPC `apply_opcoes_batch`, com validação de que o `opcao_id` recebido pertence ao `conjunto_opcao_id` do item), CSS renomeado (`classificacao-*` → `escolha-*`, `--nf` → `--na`), `item-classificacao-form.tsx` renomeado pra `item-escolha-form.tsx` (opções agora vêm do banco via prop `opcoes`, não mais de uma constante fixa), `item-medicao-form.tsx` com legenda de unidade dinâmica (`unidade_medicao`, não mais só µm), `batch-apply-panel.tsx` adaptado (bloqueio de foto obrigatória agora lê `opcoes.exige_foto` por opção, não mais um `'ruim'` fixo), as 2 páginas de fetch (`page.tsx` do item e do grupo + `layout.tsx` da nav) religadas ao schema novo. **Decisões de escopo fechadas via `brainstorming`:** só tipos `escolha`+`medição` (tipos `texto`/`data` ficam pra Peça 2, quando houver item real desse tipo); `confirm()` de N.A. removido sem substituto (sem tratamento especial por opção); badge de status do grupo colapsa de 3 estados (pendente/NF/feito) pra 2 (pendente/feito). Revisão final (whole-branch, modelo mais capaz) sem achados Crítico/Importante — só 3 Minor, 2 deixados de propósito (YAGNI, já blindados pelos triggers do banco) e 1 corrigido na hora (variável CSS órfã). 87/87 testes passando, `tsc` limpo, **verificado ponta a ponta no navegador** contra o banco remoto real (conta técnico de teste): opções carregadas do banco, salvar+redirecionar+persistir, N.A. sem diálogo, lote de irmãos com bloqueio de foto nomeando as linhas certas, item de medição com legenda de unidade dinâmica — zero erros de console. Mesclada em `main` via `finishing-a-development-branch` (merge local) em 2026-07-23; branch e worktree removidos. Design: `docs/superpowers/specs/2026-07-23-adaptar-camada-app-schema-generico-design.md`, plano: `docs/superpowers/plans/2026-07-23-adaptar-camada-app-schema-generico.md`.
- **Fase 2.8, Peça 2 — Re-seed do checklist real v7, concluída (2026-07-27).** 3 tasks via `subagent-driven-development`: um script novo (`scripts/generate_checklist_seed_v7.py`) parseia `docs/data/checklist-inspecta-v7.md` direto (sem CSV intermediário) e gera a migration `00037_seed_checklist_v7.sql` — apaga o seed antigo (320 itens/12 grupos) e insere 359 itens reais em 13 grupos (grupo 13, Motoriz. Especial, segue inativo), consolida os ~30 rótulos de "Tipo de Resposta" do documento em 22 `conjuntos_opcao` compartilhados, e recura `grupo_replicacao` (aplicar aos demais) do zero pros ~360 itens com uma regra única (nunca mistura dianteiro/traseiro — decisão deliberada, mais conservadora que a curadoria manual do v6 que às vezes misturava). **Achado real durante o brainstorming:** o rótulo "Sim/Não" sozinho não basta — 33 itens usam `Sim` pra sinalizar defeito (exige foto) e 18 pra sinalizar presença boa (não exige), então o catálogo precisou de 4 conjuntos (`sim_nao_problema`/`_presenca`, cada um com variante `_na`) em vez de 2, resolvidos por uma lista de override por nome de item, não pelo rótulo. **2 bugs reais encontrados e corrigidos durante a execução (Task 2):** `checklist_item_responses` (34 linhas de teste da QA da Fase 2) bloqueava o delete de templates via FK sem `ON DELETE` — corrigido adicionando o delete dessa tabela antes (autorizado pelo usuário, só dado de teste); um item de medição (degradação de bateria BEV) ficou sem `unidade_medicao` porque o dicionário de faixas só cobria itens com limiar numérico — corrigido + guarda de regressão. **Revisão final (whole-branch, modelo mais capaz)** encontrou 1 Importante (a unidade de medição tinha duas fontes de verdade — `TIPO_MAP` e o dicionário de faixas — causa raiz do bug acima; refatorado pra vir só de `TIPO_MAP`) e confirmou 1 Minor de conteúdo real (item #213 "Tubagens de admissão" tinha a polaridade errada, inconsistente com todo o resto dos itens de "fugas" — corrigido). Migration aplicada no banco remoto real, teste SQL 11/11 passando. Limpeza dos artefatos do seed antigo (`generate_checklist_seed.py`, `generate_grupo_replicacao_seed.py`, `checklist-inspecta-v5.csv`). **Achado colateral:** o worktree manual (`.worktrees/`, usado porque a ferramenta nativa de worktree parte de `origin/main`, que estava 34+ commits desatualizado) fazia o `vitest` rodar cada teste em dobro — `vitest.config.ts` só excluía `.claude/worktrees/`, não `.worktrees/`; corrigido. 87/87 testes passando, `tsc` limpo. Mesclada em `main` via `finishing-a-development-branch` (merge local) em 2026-07-27; branch e worktree removidos. Design: `docs/superpowers/specs/2026-07-24-reseed-checklist-v7-design.md`, plano: `docs/superpowers/plans/2026-07-24-reseed-checklist-v7.md`.
- **Fase 2.8, Peça 3 (recorte 1) — Redesign visual da tela de preenchimento (tabela densa por subseção), concluída (2026-07-28).** 7 tasks via `subagent-driven-development` (sidebar aninhada com contadores por subcategoria, `saveTextoAction`/`saveDataAction` novas preenchendo um gap real — itens `texto`/`data` nunca tiveram form próprio —, CSS da tabela/diálogos, componente `ChecklistItemTable` reunindo tudo, reescrita de `page.tsx`, atualização do `PRODUCT.md`) mais um recorte adicionado no pre-flight scan (spec pedia a atualização do `PRODUCT.md`, plano não tinha task pra isso — usuário escolheu adicionar). Cada task revisada individualmente, aprovada. **Revisão final (whole-branch, modelo mais capaz)** encontrou 2 Importantes: diálogo de medição sem forma de fechar sem submeter (armadilha real em tablet, sem toque no fundo nem Esc alcançável) e comportamento de refresh/fechamento de diálogo após salvar não verificado — corrigidos (botão Cancelar no diálogo, `dirty-check` no blur de texto/data) e re-revisados limpo. **Verificação ponta a ponta no navegador (obrigatória, §6 do design)** confirmou que salvar em qualquer linha atualiza a tabela e os contadores da sidebar sem reload manual, RF-16 bloqueia corretamente dentro do diálogo de medição, mas **achou um bug real que a revisão de código não pegou**: o diálogo de "aplicar aos demais" (família) não fechava depois de aplicar com sucesso — `BatchApplyPanel` usa `router.push` (navegação client-side) em vez do `redirect()` de Server Action que fecha o diálogo de medição automaticamente; corrigido com um callback `onSuccess` opcional. **Depois disso, o usuário testou pessoalmente no navegador e trouxe 4 achados reais**, incorporados no mesmo ciclo: (1) bug real — não tinha como anexar foto numa linha de escolha bloqueada por RF-16, corrigido (mostra `PhotoManager` reaproveitado quando a opção selecionada exige foto, mais botão "Tentar novamente"); (2) o "piscar" da página a cada resposta salva — decisão consciente do usuário de **não corrigir agora**, fica pra Fase 3 (autosave); (3) pedido novo — sidebar virou acordeão (só o grupo ativo mostra as subcategorias, resto colapsado), implementado com um Client Component (`usePathname()`) já que o layout Server Component não enxerga o `groupId` do filho; (4) gap já documentado no plano — cor dos botões de escolha só funcionava por coincidência de rótulo ("Médio"), agora calculada por posição (`ordem`) dentro do conjunto (primeiro=verde, último=vermelho, meio=âmbar, "N.A." sempre neutro) — 1 rodada de correção depois que a revisão achou que a detecção de N.A. por igualdade exata perdia a variante real "N.A. (gasolina)". 120/120 testes passando, `tsc` limpo, zero erros de console em toda a verificação manual. Mesclada em `main` via `finishing-a-development-branch` (merge local fast-forward) em 2026-07-28; branch e worktree removidos. **Recorte 2 (tabs de dados do veículo, tela de login) fica pra depois** — fora de escopo deste recorte por decisão explícita no design doc. Design: `docs/superpowers/specs/2026-07-27-redesign-tabela-subseccao-design.md`, plano: `docs/superpowers/plans/2026-07-27-redesign-tabela-subseccao.md`.
- **Fase 2.8, Peça 3 (recorte 2) — Abas de dados do veículo, concluída (2026-07-28).** 4 tasks via `subagent-driven-development`, cada uma revisada individualmente, aprovadas: mapa puro campo→aba (`lib/inspection/tabs.ts`), `createInspectionAction` devolve qual campo falhou na validação, CSS das abas, reescrita de `new-inspection-form.tsx` (formulário único de "nova inspeção" vira 5 abas — Cliente/Identificação/Histórico/Especificações/Equipamentos — sem tela nova, sem mudar o envio único no final). **Achado real durante o brainstorming, reproduzido ao vivo:** o React reseta automaticamente qualquer `<form>` ligado a uma Server Action depois de qualquer envio (sucesso ou erro), o que apagava o valor digitado em todo campo não-controlado (16 dos 22 campos do formulário). Corrigido nesta mesma peça — todo campo virou controlado, resolvendo ao mesmo tempo a persistência entre abas. **Gotcha de CSS real, não hipotético:** o atributo `hidden` e uma classe própria do painel de aba disputam a mesma especificidade pra `display` — sem uma regra `[hidden] { display: none }` explícita, trocar de aba simplesmente não escondia as outras; blindado na própria Task 3. **Revisão final (whole-branch, modelo mais capaz)** sem achados Crítico/Importante; 1 achado da Task 4 (23 `useState` separados, 1 por campo — sugestão do revisor de virar objeto de estado único) foi decisão do usuário manter como está, seguindo o padrão já usado em outros formulários do app. 130/130 testes passando, `tsc` limpo, **verificado ponta a ponta no navegador**: as 5 abas preservam valor ao trocar; erro do servidor troca de aba sozinho pro campo certo sem apagar nada; fluxo de cliente stand intacto dentro da aba Cliente; envio com sucesso cria a inspeção e redireciona — zero erros de console. Mesclada em `main` via `finishing-a-development-branch` (merge local fast-forward) em 2026-07-28; branch e worktree removidos. Auditoria dos outros formulários do app pelo mesmo bug de perda de valor fica pra depois, como item separado. Design: `docs/superpowers/specs/2026-07-28-abas-dados-veiculo-design.md`, plano: `docs/superpowers/plans/2026-07-28-abas-dados-veiculo.md`.
- **Fase 2.8, Peça 3 recorte 2 — ajuste pós-teste ao vivo, concluído (2026-07-28).** Usuário testou a versão mesclada e trouxe 2 pontos reais, resolvidos numa branch pequena à parte: (1) o botão único "Guardar" virou "Próximo" nas 4 primeiras abas (só navegação, valida com `reportValidity()` escopado à aba ativa via `[role=tabpanel]:not([hidden])`, sem depender do form inteiro por causa de uma divergência real entre jsdom e navegador real na exclusão de campos escondidos da validação nativa) — só a última aba (Equipamentos) continua submetendo de verdade; (2) a barra de abas quebrava linha (Equipamentos sozinha embaixo) porque o contêiner da página tem `max-width: 640px` — trocado `flex-wrap: wrap` por rolagem horizontal (`overflow-x: auto` + `flex-shrink: 0` nos botões). 132/132 testes, `tsc` limpo, verificado no navegador em 3 larguras (700px, 380px, fluxo completo pelas 5 abas até o envio) — zero erros de console. 1 achado Menor anotado (clicar direto na aba do topo pula a validação do "Próximo", mas o servidor ainda pega o campo faltante — sem risco de dado, fora do escopo pedido). Mesclada em `main` via merge local fast-forward.
- **Fase 2.8, Peça 3 recorte 3 — Campos reais nas abas Histórico e Equipamentos, concluída (2026-07-30).** 8 tasks via `subagent-driven-development`: 8 campos novos em `vehicle_data` pra Histórico (indícios de adulteração, proprietários anteriores, acidentes, manutenção, IPO, situação fiscal); catálogo fixo de equipamentos em código (5 categorias, 41 itens, `lib/equipamento/catalog.ts`); tabelas novas `equipamento_sugestoes`/`equipamento_inspecao`/`equipamento_fotos` (sugestões de item personalizado compartilhadas entre todos os técnicos) com RLS; aba Equipamentos com acordeões por categoria, condição (✓ Bom/⚠️ Atenção obrigatória), comentário + até 2 fotos quando "Atenção", diálogo de item personalizado; limpeza de 41 itens do checklist agora cobertos pela aba nova. **Revisão final (whole-branch)** encontrou e corrigiu 6 achados antes do merge: limite de tamanho de Server Action insuficiente pra foto real de telemóvel (1MB→12MB), sugestões de equipamento personalizado não estavam sendo carregadas de volta no formulário (feature cross-técnico não funcionava), validação nativa faltando no diálogo de item personalizado, equipamentos com condição/nome inválidos não eram filtrados antes do RPC (risco de abortar a transação inteira), estado do diálogo não resetava no fechamento nativo (Esc/backdrop), `ordem` de `equipamento_fotos` sempre `null`. 165/165 testes, `tsc` limpo. Design: `docs/superpowers/specs/2026-07-28-peca3-recorte3-historico-equipamentos-design.md`, plano: `docs/superpowers/plans/2026-07-28-peca3-recorte3-historico-equipamentos.md`.
- **Fase 2.8, Peça 3 recorte 3 — ajustes pós-teste ao vivo, concluído (2026-07-30).** Usuário testou a versão mesclada e pediu 2 mudanças, desenhadas via `brainstorming`→`writing-plans` e implementadas em 9 tasks via `subagent-driven-development`: **Histórico** — "situação fiscal regular" vira texto livre (era checkbox), "indícios de adulteração" vira gate Sim/Não com anotação condicional (era texto aberto), bloco novo "veículo importado?" (país de origem, matrícula estrangeira, data de importação, COC, isenção ISV, número DAV, todo condicional), mais data da primeira matrícula e valor base do IUC anual (`ValorMoedaInput`, máscara de moeda pt-PT nativa via `Intl.NumberFormat`). **Equipamentos** — categorias começam fechadas por padrão, cada item compacta sozinho num resumo (`"Nome — ✓ Bom"`) ao perder o foco depois de escolher a condição (reabre ao clicar), badge `"✓ X/Y verificados"` no cabeçalho de cada categoria fechada. Migration `00040`. **Revisão final (whole-branch, modelo mais capaz)** encontrou 2 Críticos reais que nenhum teste automatizado detectava — bloco de importação nunca ficava de fato escondido (`.form-grid` define `display:grid`, que vence o `[hidden]` do UA stylesheet por origem de cascata, mesmo bug já visto e documentado no recorte 2) e `ValorMoedaInput` submetia vírgula decimal (`"145,50"`) que o `z.coerce.number()` do Zod rejeita — e 2 Importantes (valores de campos condicionais ficavam "presos" no FormData ao trocar a resposta pra "Não"; item desmarcado em Equipamentos podia continuar mostrando o resumo compactado) — todos corrigidos com teste de cobertura e re-revisados limpo. `ponytail-review` achou 1 duplicação trivial de CSS (`.sim-nao-radio`/`.equip-item__condicao`), corrigida. 206/206 testes, `tsc` limpo, **verificado ao vivo no navegador pelo usuário**. Mesclada em `main` via `finishing-a-development-branch` (merge local fast-forward) em 2026-07-30; branch e worktree removidos. Design: `docs/superpowers/specs/2026-07-30-historico-equipamentos-ajustes-design.md`, plano: `docs/superpowers/plans/2026-07-30-historico-equipamentos-ajustes.md`.
- **Fase 3 — Autosave online (auditoria e correção), concluída (2026-07-30).** Auditoria (via `brainstorming`) achou RF-25/26/27 majoritariamente já implementados desde as fases anteriores; o trabalho real foi corrigir um `redirect(nextUrl)` vestigial (resquício do fluxo "uma página por item" de antes da Peça 3 recorte 1) que redirecionava a tabela densa do checklist pra si mesma a cada resposta salva — causa raiz do "piscar" já registrado como pendência. 3 tasks via `subagent-driven-development`: as 4 Server Actions de salvar item (escolha/texto/data/medição) trocam `redirect` por retorno de sucesso simples; `ItemMedicaoForm` fecha o diálogo via callback `onSuccess` explícito em vez de depender do efeito colateral do redirect; a tabela chama `router.refresh()` no sucesso (sem navegar, sem piscar) e texto/data ganham o mesmo botão "Tentar novamente" que escolha já tinha. **Revisão final (whole-branch, modelo mais capaz)** encontrou 1 Crítico real vindo do próprio design (não do implementador) — as dependências do `useEffect` do `onSuccess` incluíam a própria função, cuja identidade muda a cada render; como o diálogo de medição nunca desmonta, isso causava um loop infinito de `router.refresh()` a cada save — corrigido (deps só em `state`) e coberto por um teste que força troca de identidade da callback. Durante a implementação, 2 descobertas fora do plano, resolvidas com o usuário: `pageUrl` não era código morto como o design assumiu (ainda alimenta `FamiliaCell`→`BatchApplyPanel`, feature separada) — mantido; e uma rota inteira órfã do fluxo antigo (`[itemId]/page.tsx` + `item-escolha-form.tsx`, sem nenhum link no app, sem teste, e agora também quebrada pela remoção do redirect) foi encontrada e apagada por decisão do usuário. 211/211 testes, `tsc` limpo, **piscar confirmado eliminado pelo usuário no navegador**. Mesclada em `main` via `finishing-a-development-branch` (merge local fast-forward) em 2026-07-30; branch e worktree removidos. **5 achados novos trazidos pelo usuário durante o teste ao vivo, fora do escopo desta fase, registrados como pendência** (ver "Pendências descobertas em teste ao vivo" abaixo). Design: `docs/superpowers/specs/2026-07-30-autosave-online-design.md`, plano: `docs/superpowers/plans/2026-07-30-autosave-online.md`.

## Pendências descobertas em teste ao vivo (2026-07-31) — triagem em 2026-08-04

Usuário testou a Fase 3 mesclada no navegador (confirmou que o piscar sumiu) e trouxe 5 achados adicionais, todos em áreas que a Fase 3 não tocou. Sessão de 2026-08-04 triou os 5 (root-cause via `systematic-debugging` em vez de plano formal, por serem pontuais) — status atualizado abaixo, item por item:

1. **Navegação Histórico → Equipamentos → Checklist quebrada — reclassificado como feature ausente, não bug (2026-08-04).** Investigação encontrou que **não existe** rota/link "Histórico" ou "Equipamentos" pra uma inspeção já criada — essas abas só existem no formulário de *criação* (`inspections/new/`). A página de detalhe de uma inspeção existente (`app/(app)/inspections/[id]/page.tsx`) tem um único link, "Ir para a checklist". O que o usuário via era provavelmente esse link caindo no primeiro grupo do checklist (`ordem=1`, "Identificação e Documentação"), cujas subcategorias internas se chamam literalmente "Identificação"/"Documentação"/"Histórico" — mesmos rótulos do wizard de criação, mas dados diferentes (checklist vs. `vehicle_data`), daí a sensação de duplicação. Usuário optou por **pular por enquanto** — vira feature nova (view de Histórico/Equipamentos pra inspeção existente) se e quando for priorizada, com seu próprio ciclo `brainstorming`→`writing-plans`→`subagent-driven-development`.
2. **CSS dos radios "Funciona"/"Não Funciona" distorce — corrigido e confirmado (2026-08-04).** Causa raiz: `.escolha-options` (container flex que guarda os pills de escolha + `PhotoManager`/erro/retry quando aparecem na mesma linha) não declarava `align-items`, herdando o default `stretch` — quando o conteúdo condicional (foto/erro) entrava na mesma linha do flex-wrap, os pills esticavam pra preencher a altura. Fix de 1 linha (`align-items: flex-start` em `app/globals.css`). Confirmado pelo usuário no navegador.
3. **Itens agrupados (replicação) não persistem/reeditam direito — 2 tentativas de fix insuficientes, reescrita do zero concluída (2026-08-04).** Duas rodadas de correção pontual na sessão de 2026-08-04 (router.push→router.refresh; reintrodução de irmãos já respondidos no painel; useEffect de resync no useState local) não resolveram o sintoma — aplicar em lote não refletia na tela até reabrir o diálogo. Decisão do usuário, registrada nesta mesma entrada: parar de patchear e reescrever a lógica de grupos do zero. Feito via `brainstorming`→`writing-plans`→`subagent-driven-development` completo: design em `docs/superpowers/specs/2026-08-04-aplicar-aos-demais-reescrita-design.md`, plano em `docs/superpowers/plans/2026-08-04-aplicar-aos-demais-reescrita.md`, executado em 5 tasks independentes (cada uma revisada em isolado antes da próxima). Mudança de mecanismo, não só de código: `BatchApplyPanel` deixou de chamar o RPC de lote (`apply_opcoes_batch`/`applyOpcoesBatchAction`, ambos removidos do código TypeScript — confirmado por grep, o RPC continua só na migration do banco) e passou a reutilizar a mesma Server Action do salvamento individual (`saveEscolhaAction`) num loop sequencial por irmão incluído — mesma confiabilidade comprovada do fluxo individual, ao custo de não ser atômico (uma falha no meio para o loop e deixa os itens anteriores já salvos, decisão aceita). O bug de sincronização visual (pill do irmão não atualizava até reabrir o diálogo) foi resolvido trocando o `useEffect` de resync do `EscolhaCell` — que não bateu nos 2 patches anteriores — por remount via `key={item.id + opcao_id}` em `ChecklistItemTable`, forçando o `useState` inicial a reler o prop fresco em vez de tentar sincronizar um estado já montado. Verificação automatizada limpa: 216/216 testes, `tsc --noEmit` sem erros. **Teste manual no navegador pelo usuário encontrou um 3º bug real, corrigido na hora (2026-08-04).** A revisão final whole-branch já tinha avisado que o remount via `key` era logicamente equivalente ao `useEffect` de resync anterior e sugeriu investigar a camada de dados se o sintoma sobrevivesse — mas a investigação ao vivo (logs temporários no servidor + captura de tela) **descartou a camada de dados por completo**: o prop `response` chegava com o valor certo (confirmado via debug lado a lado — `useState` local e prop idênticos nos dois itens irmãos), o `conjunto_opcao_id`/catálogo de opções também batia entre os dois itens, e o próprio `router.refresh()` buscava dados frescos corretamente a cada chamada. O bug real: o `<input type="radio">` recém-remontado via `key` recebia o `opcao_id` certo em React, mas o navegador não repintava o estado nativo `checked` — reproduzível só no caminho de remount (nunca num reload completo da página, nunca depois de um clique real do usuário). Corrigido forçando `el.checked` de forma imperativa via `ref` callback logo após o input montar, além do `checked` declarativo já existente — resolve o repaint sem abrir mão do remount via `key`. **Confirmado funcionando pelo usuário ao vivo no navegador.** Esse tipo de bug de repaint nativo não é capturado pelo jsdom (a suíte automatizada já passava antes e depois do fix), então a verificação manual no navegador continua sendo o único jeito de pegar essa classe de problema — vale lembrar disso da próxima vez que um `key`-remount envolver `<input>` nativo.
4. **Badge de grupo só aparece depois de responder — corrigido e confirmado (2026-08-04).** O dado de `grupo_replicacao` já estava disponível no item antes de qualquer resposta (vem direto do template, não da resposta) — só o gate de renderização (`response?.respondido === true`) escondia o indicador até depois. Agora mostra um badge passivo (👪, opacidade reduzida) assim que o item carrega; vira o botão interativo de aplicar-em-lote só depois de respondido. Confirmado pelo usuário no navegador.
5. **Delay visual (cinza → preto) ao selecionar um item — investigado e corrigido (2026-08-04).** Veredito da investigação: **não** era regressão da Fase 3 nem timing de CSS — era ausência real de UI otimista (o estado "respondido" da linha sempre esperou o round-trip completo servidor→`router.refresh()`, desde antes da Fase 3; ela só trocou o mecanismo de redirect-pra-si-mesmo por `router.refresh()`, e coincidentemente removeu o "flash" de remount total que antes mascarava o delay). Corrigido com estado otimista local (`ChecklistItemTable` marca o item como respondido no clique, antes do round-trip; reverte se o servidor recusar). Confirmado pelo usuário no navegador.

**Achados extras da sessão de 2026-08-04, fora da lista original:**
- **Botão "Guardar" do formulário de nova inspeção falhava silenciosamente** quando um item de equipamento selecionado ficava sem "condição" (✓ Bom/⚠️ Atenção) escolhida — o navegador bloqueava o envio via validação nativa sem indicar o campo, ao contrário do botão "Próximo" (que já usava `reportValidity()` escopado). Corrigido replicando o mesmo padrão no clique do "Guardar". **Causa real do "nada acontece" que o usuário reportou em cima disso**, descoberta à parte: extensão de navegador (LanguageTool) injetando atributos na tag `<html>` antes da hidratação do React, quebrando a interatividade da página inteira — não era bug do app; confirmado testando em janela anônima (sem extensões), onde o fluxo completo funcionou e a inspeção foi criada no banco com sucesso.
- **Botão "Preencher com dados de teste"** adicionado ao formulário de nova inspeção (só em `NODE_ENV !== "production"`, nunca vai pra build de produção) — preenche todos os campos com valores aleatórios/plausíveis, incluindo uma amostra de itens de equipamento com condição "Bom", pra acelerar teste manual repetido.
- **`PhotoManager` redesenhado** (usado em 3 lugares: checklist, aplicar-em-lote, formulário de medição) a pedido do usuário: o input nativo de arquivo (botão grande "Escolher ficheiro" + texto "Nenhum ficheiro selecionado") virou um botão circular só com ícone 📷, do mesmo tamanho dos pills de escolha, sentado na mesma linha deles; miniaturas de fotos já anexadas ficam pequenas (48×36) abaixo, e clicar numa abre um lightbox nativo (`<dialog>`) com a foto ampliada. Ainda não confirmado pelo usuário no navegador (pendente de teste).

## Estado atual

Todo o bloco 0 (housekeeping) está concluído — ver seção Progresso acima. `main` tem: 45 migrations (schema genérico de tipos de resposta — `conjuntos_opcao`/`opcoes`/`medicoes` —, re-seed real da Peça 2, os campos/tabelas novos da Peça 3 recorte 3 + ajustes, as 3 views de pontuação da Fase 4, e o trigger de transição de status da Fase 5 sub-projeto 1), RLS nas tabelas + policy de Storage, checklist seedado com o conteúdo real (318 itens/13 grupos após a limpeza dos 41 itens cobertos pela aba Equipamentos), código de app da Fase 1a (login + dados básicos), Fase 1 (navegação do checklist, PR #3 mesclada), validade da inspeção (PR #2 mesclada), Fase 2 (preenchimento de item, PR #4 mesclada, já adaptada ao schema genérico pela Peça 1b), Fase 2.5 (aplicar aos demais, mesclada e enviada, idem), Fase 2.6 (rename Check Auto, mesclada), Fase 2.7 (design system aplicado a todas as telas, mesclada e enviada — porém **rejeitada pelo usuário depois de ver rodando**, ver Fase 2.8) **Fase 2.8 completa** (Peças 1a/1b/2/3, todos os recortes de Peça 3 incluindo o 3 e seus ajustes pós-teste, mescladas), **Fase 3 completa** (autosave — correção do redirect vestigial/piscar, retry padronizado, mescladas), **Fase 4 completa** (pontuação) e **Fase 5 sub-projeto 1 completo** (finalização do técnico — ver seção "Fase 5" abaixo). `npm test` limpo em `main` (235/235), `tsc` limpo, docs sincronizados. Enviado pro GitHub (`origin/main` em dia). Diretório de trabalho é `~/Desktop/bild app` (KINGSTON está corrompido/deprecated — nunca usar; **cuidado extra**: o harness reseta o cwd do shell pra dentro do KINGSTON entre alguns comandos Bash, então qualquer script que assuma "ainda estou onde dei `cd` antes" precisa recomeçar com um `cd ~/Desktop/"bild app"` explícito logo no início do próprio comando, não só uma vez no início da sessão).

**Fase 2.8 (redesign do checklist) — ✅ completa (2026-07-30).** Peça 1a (schema genérico), Peça 1b (camada de app adaptada ao schema novo), Peça 2 (re-seed do checklist real), Peça 3 recorte 1 (redesign visual da tela de preenchimento), Peça 3 recorte 2 (abas de dados do veículo + ajuste pós-teste) e Peça 3 recorte 3 (campos reais em Histórico/Equipamentos + ajustes pós-teste) — todas concluídas, revisadas e mescladas em `main` (2026-07-23 a 2026-07-30) — ver detalhes na seção Progresso acima.

**Fase 3 (autosave online) — ✅ completa (2026-07-30).** Ver detalhes na seção Progresso acima. **Triagem das 5 pendências feita em 2026-08-04** (ver "Pendências descobertas em teste ao vivo" acima): 3 corrigidas e confirmadas no navegador (CSS dos radios, badge de grupo tardio, delay visual/UI otimista), 1 reclassificada como feature ausente e adiada por decisão do usuário (navegação Histórico/Equipamentos pra inspeção existente), 1 (aplicar-em-lote) reescrita do zero via ciclo completo `brainstorming`→`writing-plans`→`subagent-driven-development` (2026-08-04), depois de 2 tentativas de patch pontual não resolverem — mesclada em `main`, **confirmada funcionando ao vivo pelo usuário** depois de um 3º fix (repaint nativo do radio após remount via `key`, ver item 3 em "Pendências descobertas em teste ao vivo" acima). **As 5 pendências da Fase 3 estão todas fechadas** (4 corrigidas + 1 adiada por decisão de escopo). Auditoria dos outros formulários do app pelo mesmo bug de perda de valor no erro (corrigido só no formulário de nova inspeção) continua como item separado, menor prioridade.

**Fase 4 (pontuação) — ✅ completa (2026-08-05).** RF-38 a RF-42 generalizados pro schema de `conjuntos_opcao` da Fase 2.8, fórmula por posição, coluna `opcoes.is_na` e as 3 views de pontuação (`checklist_item_score`, `checklist_group_score`, `inspection_score`) — todas verificadas contra a base real pelo usuário. Ver detalhes na seção "Fase 4 — Pontuação" abaixo.

**Fase 5 completa e mesclada em `main` (2026-08-07).** Sub-projeto 1 (finalização do técnico) e sub-projeto 2+3 (lista/aprovação/edição/auditoria/cancelamento do admin + gestão de técnico) — ambos em `main`. Ver detalhes na seção "Fase 5" abaixo. Migration `00046` (policy de INSERT em `public.users`, achado real da revisão final) ainda precisa ser aplicada manualmente no Supabase, como as outras.

**Fase 5, sub-projeto 1 (finalização do técnico) — ✅ completo (2026-08-06).** RF-23/24 e RF-33/34. Ver detalhes na seção "Fase 5" abaixo.

**Estado real em 2026-08-14:** todos os itens acima estão em `main`, incluindo Fase 5 sub-projeto 2+3, "Remoção da duplicação Identificação/Histórico" e **Fase 6 (relatório final)** — as 3 últimas entradas deste doc ficaram por um tempo marcadas como "aguardando merge" mesmo já mescladas (fast-forward local sem atualizar o texto na hora); corrigido nesta atualização. `npm test` limpo (329/329), `tsc --noEmit` limpo, `npm run build` limpo. `main` ainda não foi enviado ao GitHub desde a Fase 6 (`origin/main` desatualizado). Migrations `00046` e `00052` seguem pendentes de aplicação manual no Supabase. Próxima fase real: **Fase 7 — Acesso do cliente**.

---

## Passo 0.1 — Integrar o único branch com app code ✅ concluído

**Prompt:**
> Pronto pra integrar o branch `worktree-fase1a-dados-basicos` (RF-01 login, RF-02–06 dados básicos) no main. Se ainda não foi revisado, use `superpowers:requesting-code-review` primeiro; depois `superpowers:finishing-a-development-branch` pra decidir o caminho de merge.

**Pronto quando:** branch integrado (merge ou PR mesclado) e `main` passa a ter código de app.

## Passo 0.2 — Encerrar branch obsoleto ✅ concluído

**Prompt:**
> As migrations 00011–00014 do branch `worktree-fase2-preenchimento-item` já estão em `main`. Verifique se esse branch tem algo além disso (testes, doc do plano) que ainda não foi trazido; se não tiver, feche/apague o branch.

**Pronto quando:** branch fechado ou confirmado como redundante.

## Passo 0.3 — Sincronizar docs desatualizados (rápido, opcional mas recomendado) ✅ concluído

**Prompt:**
> `docs/README.md` e `docs/database-schema-v1.md` estão desatualizados — dizem que não há código de app nem RLS, e ambos já existem. Atualize os dois pra refletir o estado real.

**Pronto quando:** os dois arquivos batem com a realidade do código.

## Passo 0.4 — Excluir arquivos `._*` da descoberta de testes do Vitest ✅ concluído

Descoberto ao rodar os testes pós-merge do Passo 0.1: este volume (KINGSTON, externo) gera sidecar files `._*` (AppleDouble) pra todo arquivo, inclusive `.test.ts`/`.test.tsx`. O Vitest tenta parsear `._algumacoisa.test.tsx` como teste e falha com erro de parse — 6 suítes fantasma na última rodada, nenhuma delas real. Vai continuar acontecendo em toda fase futura até isso ser excluído no `vitest.config.ts`.

**Prompt:**
> Os arquivos `._*` (AppleDouble, macOS) estão sendo pegos pela descoberta de testes do Vitest e gerando suítes fantasma com erro de parse. Adicione `**/._*` ao `exclude` de `vitest.config.ts` e confirme que `npm test` fica limpo.

**Pronto quando:** `npm test` roda sem nenhuma suíte `._*` aparecendo.

---

## Fase 1 (restante) — Navegação do checklist ✅ concluída (2026-07-20)

RF-09 a RF-12: lista de grupos, indicadores de progresso.

Implementado, verificado no navegador, revisado (code review + ponytail-review), mesclado em `main` via PR #3.

## Fase 2 (UI) — Preenchimento de item ⏳ design aprovado, plano de implementação a seguir

RF-13 a RF-22. DB layer já pronta em `main`: contagem de pontos, `resultado_calculado` gerado, trigger que exige foto quando resultado é "ruim", policy de DELETE de foto. Design: `docs/superpowers/specs/2026-07-20-preenchimento-item-design.md`. Inclui gap novo descoberto e resolvido no design: bucket de Storage pra fotos não existia em nenhuma migration.

**Prompt:**
> Segue `superpowers:writing-plans` a partir de `docs/superpowers/specs/2026-07-20-preenchimento-item-design.md`.

## Fase 2.5 — "Aplicar aos demais" em itens repetidos ✅ implementada e revisada, aguardando merge (2026-07-21)

Resolve a pendência de `docs/especificacao-tecnica-v1.md` §6. Ver detalhes na seção Progresso acima. Design: `docs/superpowers/specs/2026-07-21-aplicar-aos-demais-design.md`, plano: `docs/superpowers/plans/2026-07-21-aplicar-aos-demais.md`.

## Fase 2.6 — Rename Inspecta → Check Auto ✅ concluída (2026-07-22)

Ver detalhes na seção Progresso acima. Design: `docs/superpowers/specs/2026-07-22-rename-check-auto-design.md`, plano: `docs/superpowers/plans/2026-07-22-rename-check-auto.md`.

## Fase 2.7 — Aplicar o sistema de design nas telas existentes ✅ concluída (2026-07-22), depois rejeitada

Ver detalhes na seção Progresso acima. Mesclada e enviada, mas o usuário rejeitou o resultado visual depois de ver rodando — ver Fase 2.8.

## Fase 2.8 — Redesign do checklist (dashboard) ✅ concluída (2026-07-30)

Rejeição da Fase 2.7 + 10 screenshots de referência (dashboard com sidebar, tabela por subseção, tabs de dados do veículo) + o documento real do checklist v7 (360 itens/13 categorias) juntos motivaram 4 peças sequenciais, cada uma com seu próprio ciclo `brainstorming → writing-plans → subagent-driven-development`:

- **Peça 1a — Modelo genérico de tipos de resposta (schema) ✅ concluída e mesclada (2026-07-23).** Ver detalhes na seção Progresso acima. Design: `docs/superpowers/specs/2026-07-22-modelo-generico-tipos-resposta-design.md`, plano: `docs/superpowers/plans/2026-07-22-modelo-generico-tipos-resposta-1a-schema.md`.
- **Peça 1b — Adaptar a camada de app ao schema novo ✅ concluída e mesclada (2026-07-23).** Ver detalhes na seção Progresso acima. Design: `docs/superpowers/specs/2026-07-23-adaptar-camada-app-schema-generico-design.md`, plano: `docs/superpowers/plans/2026-07-23-adaptar-camada-app-schema-generico.md`.
- **Peça 2 — Re-seed do checklist real (360 itens/13 categorias) ✅ concluída e mesclada (2026-07-27).** Ver detalhes na seção Progresso acima. Design: `docs/superpowers/specs/2026-07-24-reseed-checklist-v7-design.md`, plano: `docs/superpowers/plans/2026-07-24-reseed-checklist-v7.md`.
- **Peça 3, recorte 1 — Tela de preenchimento (tabela densa por subseção) ✅ concluída e mesclada (2026-07-28).** Ver detalhes na seção Progresso acima. Design: `docs/superpowers/specs/2026-07-27-redesign-tabela-subseccao-design.md`, plano: `docs/superpowers/plans/2026-07-27-redesign-tabela-subseccao.md`.
- **Peça 3, recorte 2 — Tabs de dados do veículo (login descartado do escopo) ✅ concluída e mesclada (2026-07-28).** Ver detalhes na seção Progresso acima. Design: `docs/superpowers/specs/2026-07-28-abas-dados-veiculo-design.md`, plano: `docs/superpowers/plans/2026-07-28-abas-dados-veiculo.md`.
- **Peça 3, recorte 3 — Campos reais nas abas Histórico e Equipamentos, mais ajustes pós-teste ✅ concluída e mesclada (2026-07-30).** Ver detalhes na seção Progresso acima (duas entradas: implementação inicial + ajustes pedidos depois do teste ao vivo). Design: `docs/superpowers/specs/2026-07-28-peca3-recorte3-historico-equipamentos-design.md` e `docs/superpowers/specs/2026-07-30-historico-equipamentos-ajustes-design.md`; planos: `docs/superpowers/plans/2026-07-28-peca3-recorte3-historico-equipamentos.md` e `docs/superpowers/plans/2026-07-30-historico-equipamentos-ajustes.md`.

Fase 2.8 completa — todas as 5 peças/recortes (incluindo os ajustes pós-teste de recorte 2 e recorte 3) concluídos e mesclados em `main`. Próxima unidade de trabalho: Fase 3 (autosave), abaixo.

## Fase 3 — Autosave online ✅ concluída (2026-07-30)

RF-25 a RF-27, RNF-22. Ver detalhes na seção Progresso acima. Design: `docs/superpowers/specs/2026-07-30-autosave-online-design.md`, plano: `docs/superpowers/plans/2026-07-30-autosave-online.md`.

## Fase 4 — Pontuação ✅ concluída (2026-08-05)

RF-38 a RF-42, RNF-18–19. O bloqueio original (valores de corte A/B/C e fórmula de pontuação indefinidos, §6 da especificação) tinha uma causa mais profunda: o texto de RF-38 a RF-42 ainda assumia a classificação fixa de 4 valores (ótimo/médio/ruim/NF) de antes da Fase 2.8, que deixou de existir quando o schema generalizou pra ~22 `conjuntos_opcao` compartilhados (2 a 5 opções cada) mais os tipos `medição`/`texto`/`data`, nenhum dos quais tem classificação de qualidade embutida. Resolvida via `brainstorming` → `writing-plans` → `subagent-driven-development` (4 tasks). **Decisões fechadas no brainstorming:** fórmula de pontuação por posição — a opção na posição *i* entre N opções válidas (não-N.A.) pontua `10 - (i-1) × 8/(N-1)`, reproduzindo exatamente o 10/6/2 do RF-38 original pra conjuntos de 3 opções e interpolando pra qualquer N; medição reaproveita o resultado ok/atenção/crítico que a Peça 1a já calcula (`medicoes_resultado`), mapeado pra essa mesma escala 10/6/2; texto/data nunca pontuam; itens N.A. e pendentes ficam de fora das médias, mesmo princípio de exclusão que o antigo "NF"; cortes de classificação A≥8/B≥5/C<5; escopo é só cálculo — sem tela nova, a Fase 5 é quem vai consumir essas views depois. Coluna nova `opcoes.is_na boolean` (migration `00041`) vira a fonte estruturada de verdade pra quais opções são N.A., substituindo (só pra pontuação — o cliente continua usando a regex) a inferência por regex que já existia em `lib/checklist/siblings.ts`; backfilada com o mesmo padrão do regex, batendo exatamente nas 6 opções N.A. reais do seed. Três views novas, todas `security_invoker = true`: `checklist_item_score` (migration `00042`, ponto por resposta individual, fórmula por posição via CTE), `checklist_group_score` e `inspection_score` (migration `00043`, agregação por grupo e por inspeção com os cortes A/B/C). Simplificação deliberada em relação ao design doc: grupos/inspeções sem nenhuma resposta simplesmente não geram linha na view (em vez de uma linha com campos nulos) — comportamento idêntico pra qualquer chamador via `.maybeSingle()`/LEFT JOIN, documentado inline na própria migration `00043`. Este ambiente não tem acesso à base de dados ao vivo, então toda migration foi verificada por revisão estática de SQL (rastreamento manual, não execução) em vez de rodar teste automatizado — mas cada migration e arquivo de teste foi depois aplicado manualmente e confirmado pelo usuário direto na base real via SQL Editor do Supabase, incluindo conferência contra dados reais (não sintéticos): `checklist_item_score` validado contra 20 itens respondidos de verdade (10/6/2 em conjuntos de 3 opções, 7.33 interpolado num conjunto de 4 opções, null num item com opção N.A. selecionada) e `inspection_score` validado contra inspeções reais (notas 6–8 classificadas 'B' em inspeções que o usuário de fato preencheu, null/null em rascunhos ainda não tocados) — tudo correto. Duas rodadas de `requesting-code-review` ao longo do caminho (Task 3 precisou de um fixture pra cobrir exatamente a fronteira 5.0 B/C e uma nota de doc sobre a simplificação de escopo) — ambas resolvidas sem achado pendente. Design: `docs/superpowers/specs/2026-08-04-pontuacao-design.md`, plano: `docs/superpowers/plans/2026-08-04-pontuacao.md`.

## Fase 5 — Revisão do admin

RF-31 a RF-37, RF-57 a RF-62. Tabelas `review_events`, `audit_log_entries` e índices de listagem já existem em `main`. O brainstorming inicial (2026-08-05) dividiu a fase em 3 sub-projetos sequenciais, porque o primeiro gap encontrado (RF-23/24, o botão de finalizar do técnico) bloqueava os outros dois — não existe nada "enviado" pro admin revisar sem ele:

1. **Finalização do técnico (RF-23/24, RF-33/34) — ✅ completo (2026-08-06).**
2. **Lista do admin + aprovação/devolução (RF-31–34, RF-57–59, RF-62) — ✅ completo (2026-08-07), fundido com o sub-projeto 3.**
3. **Edição do admin + auditoria + cancelamento (RF-35–37, RF-60–61) — ✅ completo (2026-08-07), ver sub-projeto 2.**

**Fase 5 completa** — todos os RF-31 a RF-37 e RF-57 a RF-62 implementados, revisados, verificados ao vivo e mesclados em `main` (branch `worktree-revisao-gestao-admin` — ver detalhes no sub-projeto 2+3 abaixo).

### Sub-projeto 1 — Finalização do técnico ✅ completo (2026-08-06)

Botão "Finalizar inspeção" / "Reenviar para aprovação" (RF-34) na página de resumo, checagem de completude no servidor (reaproveitando `computeGroupProgress`, sem confiar só no estado desabilitado do botão no client), e read-only na checklist inteira quando o status não permite mais edição — a RLS (`owns_editable_inspection()`, migration `00008`) já bloqueava a escrita, faltava só a UI espelhar essa condição em vez de deixar o técnico bater num erro cru.

Implementado via `brainstorming` → `writing-plans` → `subagent-driven-development` (4 tasks, cada uma revisada). Revisão final whole-branch (2026-08-06, antes do merge) encontrou 1 achado Importante real e pré-existente fora do diff da branch, não bloqueante mas corrigido no mesmo ciclo por decisão do usuário: a policy `inspections_update` (migration `00008`) validava no `WITH CHECK` só *quem* podia escrever (`tecnico_id = auth.uid()`), nunca *pra que status* — um técnico podia, via chamada direta ao Supabase fora do app, pular `status` direto pra `'aprovada'`/`'cancelada'`, ignorando por completo a revisão do admin que a Fase 5 existe pra garantir. Apertar o `WITH CHECK` diretamente quebraria a edição normal (técnico edita outros campos numa inspeção em rascunho sem nunca tocar `status`, e `WITH CHECK` não enxerga o valor antigo da coluna pra distinguir os dois casos) — resolvido com um trigger `BEFORE UPDATE` (mesmo padrão já usado pro RF-16 em `00033_rf16_generico.sql`), que só permite ao técnico a transição `rascunho`/`devolvida` → `aguardando_aprovacao`; qualquer outra mudança de status por um não-admin é bloqueada. Migration `00045`, aplicada e testada contra a base real pelo usuário.

Outros achados da revisão (também corrigidos no mesmo ciclo): cobertura de teste faltando pro gate `editable=false` por tipo de célula da tabela do checklist e pro guard de grupos vazios na Server Action; badge "Só leitura" e banner de devolução reaproveitando um par de cor (`amber-100`/`600`) já documentado como reprovado em contraste AA — trocado pro `amber-700` que já existia na mesma branch; badge "Só leitura" explicava o motivo só via `title` (não dispara em touch, app é tablet-first) — virou legenda sempre visível.

**Trabalho adicional incorporado no mesmo ciclo, a pedido do usuário:** rebrand de cor (verde da marca pra Tailwind emerald, preto de identidade pra um tom escuro) e, depois, um redesign da sidebar do checklist a partir de um PDF de auditoria UI/UX que o usuário trouxe (fundo Slate Dark `#0F172A` literal — substituindo a primeira tentativa de rebrand, que tinha corrigido o hue pro verde sem isso ter sido pedido —, badges de pendência sem o bege/âmbar que destoava da paleta, conector `↳` nas subcategorias). Isolado numa branch própria (`sidebar-redesign`, a partir do topo da branch já revisada) especificamente pra não arriscar o trabalho já aprovado; testado ao vivo pelo usuário no navegador, um ajuste pedido (indicador de progresso: manteve o número decrescente com `✓` no final, em vez do formato `x/total` que a PDF sugeria) e então mesclado de volta via fast-forward.

235/235 testes, `tsc` limpo. Mesclada em `main` via `finishing-a-development-branch` (fast-forward local) e enviada pro GitHub em 2026-08-06. Design: `docs/superpowers/specs/2026-08-05-finalizacao-tecnico-design.md`, plano: `docs/superpowers/plans/2026-08-05-finalizacao-tecnico.md`.

### Sub-projeto 2+3 — Revisão e gestão do admin ✅ completo (2026-08-07)

RF-31–37, RF-57–62. Fundidos num ciclo só no brainstorming — a RLS já suportava edição do admin desde a Fase 1 (`is_admin()` já tinha bypass em `inspections`, `checklist_item_responses`, `vehicle_data`, `photos`), então o custo marginal de incluir edição/auditoria/cancelamento junto com lista/aprovação era baixo, e evitava duas rodadas de revisão sobre a mesma tela de detalhe. 12 tasks via `subagent-driven-development`: `getCurrentUser()` compartilhado (id + papel, base de todo o resto), `isInspectionEditable(status, role)` fica ciente de papel (admin sempre edita, técnico segue a regra de sempre), lista "Minhas Inspeções" do técnico (novo destino pós-login), lista "Todas as inspeções" do admin (busca/filtro/ordenação client-side, "atrasada" via a view `inspections_with_flags` da Fase 1 — nunca usada até agora), roteamento por papel (login redireciona por `role`, guard em `/admin`, guards nos dois pontos de entrada só-de-técnico, middleware cobrindo `/admin`), log de auditoria (RF-36) nas 6 Server Actions de item do checklist (só dispara quando quem chama é admin), aprovar/devolver/cancelar unificados num `AdminActionsPanel`, botão de relatório placeholder (Fase 6 constrói o de verdade), aviso quando o admin edita uma inspeção já aprovada (a nota recalcula ao vivo, view não é snapshot), Histórico admin-only (`review_events` + `audit_log_entries` mesclados), e gestão de técnico em `/admin/tecnicos` (criar via `auth.admin.createUser`, desativar/reativar via ban nativo do Supabase Auth — sem coluna `ativo` nova).

**Revisão final whole-branch (2026-08-07, modelo mais capaz) encontrou 2 Críticos reais, ambos corrigidos no mesmo ciclo:** `public.users` tinha RLS habilitada desde a migration `00008` mas nunca ganhou policy de INSERT — o insert de perfil em `createTecnicoAction` falhava com `42501` em toda chamada, então "criar técnico" nunca funcionou de fato (migration `00046`, admin-only); e um loop de redirect infinito entre `/inspections` e `/admin` pra qualquer usuário autenticado sem linha em `public.users` (`getCurrentUser()` retorna `null` tanto pra "sem sessão" quanto pra "sessão sem perfil", e os dois guards mandavam esse caso um pro outro) — corrigido mandando o caso `!user` do guard de `/admin` pro `/login` em vez de `/inspections`, quebrando o ciclo no único nó compartilhado. Mais 3 Importantes: `SUPABASE_SERVICE_ROLE_KEY` não documentada em lugar nenhum (`/admin/tecnicos` quebrava com "supabaseKey is required" na primeira visita); embed de `vehicle_data` tratado de dois jeitos diferentes entre a lista do técnico (com `Array.isArray` defensivo mas inútil) e a do admin (sem guard nenhum) pra exatamente a mesma relação 1:1; `getCurrentUser()` sem `cache()`, gerando round-trips duplicados quando layout+página chamam no mesmo request. Durante o próprio fix, o controller (não o reviewer) achou mais 1 bug real no teste SQL novo da migration `00046` — as linhas de `public.users` inseridas no teste não tinham `auth.users` correspondente (FK), o que faria o teste falhar por violação de FK em vez de testar a policy — corrigido antes do re-review escopado fechar limpo.

**`ponytail-review` (2026-08-07)** achou 2 pontos reais no diff inteiro da branch: tipo `UserRole` duplicado entre `lib/inspection/status.ts` e `lib/auth/session.ts` (import em vez de redeclarar); `atrasada` (RF-62) recalculada em TS com um `today` derivado em UTC no client, duplicando a view `inspections_with_flags` da Fase 1 (nunca usada, calcula a mesma coisa com `current_date` no servidor) — os dois corrigidos, e a troca pra view testada diretamente contra o PostgREST real (não só localmente) antes de aceitar.

**Teste ao vivo pelo usuário** (criou a primeira conta admin real do projeto, `gestor@checkauto.pt` — `admin@checkauto.pt` foi rejeitado pelo Supabase Auth por validação de email, domínio `checkauto.pt` não tem infraestrutura de entrega real então o signup público também não funciona, conta criada direto no Dashboard) encontrou 2 gaps de navegação reais que nenhuma revisão tinha pego: `/admin/tecnicos` e `/inspections/[id]` (destino de todo clique em "Ver" na lista do admin) não tinham nenhum link de volta. Resolvidos junto com uma passada `impeccable` nas telas novas: removida uma borda lateral (`border-left`) que é o "AI-tell" mais reconhecível segundo a própria ferramenta; `.stack-row` (pensada pra grupo de botão de diálogo, `justify-content: flex-end`) estava sendo reaproveitada sem querer pra título+ação de página e pra barra de busca/filtro, empurrando os dois pra direita da tela — duas classes novas (`.page-header`, `.filter-bar`) resolvem cada caso; `.page` (640px, pensada pro formulário do técnico) espremia a tabela de 7 colunas do admin — nova variante `.page--wide`.

273/273 testes, `tsc` limpo. Migration `00046` (+ teste) ainda precisa ser aplicada manualmente no Supabase, mesmo processo de sempre. Branch `worktree-revisao-gestao-admin` implementada, revisada, testada ao vivo e mesclada em `main`. Design: `docs/superpowers/specs/2026-08-06-revisao-gestao-admin-design.md`, plano: `docs/superpowers/plans/2026-08-06-revisao-gestao-admin.md`.

**Segunda rodada de polish de UI (2026-08-09), a pedido do usuário, com as skills `frontend-design` e `web-design-guidelines`:** teste ao vivo de `/inspections` (lista do técnico) e `/admin` (lista do admin) encontrou uma lista de problemas visuais reais — layout centralizado cansativo, sem estrutura visual, badges de estado inconsistentes, tabela do admin comprimida, filtro sem padrão visual. O pedido inicial incluía redesign maior (sidebar de navegação, cards) — como isso mexeria no app shell inteiro e não só nas duas telas, disparou `brainstorming` (mandatório pra feature nova, ver topo deste doc), mas o usuário interrompeu pra pedir uma passada mais enxuta: manter listas/tabelas (sem cards), gastando poucos tokens. Ficou fora do escopo desta rodada — cards continuam como alternativa se esta direção não for suficiente.

Mudanças aplicadas, todas reaproveitando tokens já existentes em `DESIGN.md`/`globals.css` (nenhum token novo, nenhuma dependência nova — `shadcn` foi avaliado e descartado por não haver `components.json` no projeto e o design system atual já ser maduro o bastante):
- **Severidade dos botões de revisão:** "Devolver" (decisão de rotina, nada se perde) usava `btn-danger`, o mesmo vermelho de "Cancelar inspeção" (ação terminal) — os dois ficavam lado a lado e confundíveis. Nova classe `.btn-warning` (âmbar) pra "Devolver"; `.admin-actions__group` separa visualmente o grupo Aprovar/Devolver do botão Cancelar.
- **Tabelas do admin:** `.table-scroll` contém o overflow horizontal na própria tabela (não mais a página inteira em telas estreitas); hover de linha (`.item-table--scannable`) e `font-variant-numeric: tabular-nums` na coluna Nota pra facilitar leitura de várias linhas; mais padding vertical que a tabela densa do checklist (que reusa a mesma classe base `.item-table`, mas com o modificador `--scannable` só nas telas de admin).
- **Lista "Minhas Inspeções" (técnico):** a pedido explícito, manteve o formato de lista (sem virar cards) — cada linha ganhou borda visível (`.item-list--bordered`) pra ler como item discreto em vez de texto solto, badge de estado colorido (`status-pill`, reaproveitando o mesmo mapeamento já usado na página de detalhe) no lugar do texto plano, e passou a mostrar marca/modelo/cor do veículo (antes só matrícula), a pedido do usuário pra dar mais contexto ao técnico.
- **Barra de filtros do admin:** virou uma superfície única (fundo/borda/padding) em vez de inputs soltos direto no fundo da página; busca ganhou ícone de lupa; selects com largura mínima consistente. O dropdown de ordenação foi trocado por um filtro de data (`<input type="date">`), a pedido do usuário — mais direto que ordenar. **Bug real encontrado e corrigido:** a seta customizada do `<select>` não tinha `background-size` definido, então o SVG (sem `width`/`height` intrínsecos) renderizava no tamanho padrão do navegador em vez de pequeno — não era só estética, era um bug de CSS.
- **Coluna "Prazo" (admin):** o aviso de atraso (RF-62, já existia embutido no texto do Estado) virou coluna própria — inicialmente nomeada "Atrasada", renomeada pra "Prazo" a pedido do usuário pra não duplicar o nome da coluna "Estado" (status do ciclo de vida da inspeção) já existente. Coluna "Estado" também ganhou os badges `status-pill` coloridos (antes texto plano).
- **Ação "Ver":** ícone de olho (`.btn--icon`, modificador novo e escopado — não alterou o `.btn` base, que outras telas do app ainda usam sem ícone) em vez de botão só-texto.

273/273 testes, `tsc` limpo (confirmado depois de cada mudança). Nenhuma migration nova. Doc/spec formal não foi escrita pra esta rodada — passada iterativa direto no código, por pedido explícito do usuário de manter o custo baixo.

## Remoção da duplicação Identificação/Histórico ✅ completo e mesclado em `main` (2026-08-10)

Achado durante teste ao vivo do usuário (não é uma das 9 fases originais): o formulário "Nova Inspeção" já coletava marca/modelo/cor/VIN/matrícula/etc. e quilometragem/histórico de manutenção/indícios de adulteração/etc., e o checklist (grupo `ordem=1`, subcategorias "Identificação"/"Histórico") pedia os mesmos 22 dados de novo — 4 deles do tipo `escolha` pontuando indevidamente na nota geral (usuário confirmou que só "Equipamentos" deveria contar nota). Desenhado via `brainstorming` → `writing-plans` → `subagent-driven-development` (8 tasks, cada uma com implementer + revisor + fix loop). Design: `docs/superpowers/specs/2026-08-10-remocao-duplicacao-identificacao-historico-design.md`, plano: `docs/superpowers/plans/2026-08-10-remocao-duplicacao-identificacao-historico.md`.

**Solução:** os 22 itens saem do checklist (migration `00047`, delete em duas etapas — respostas antes dos templates, sem `on delete cascade`). Correção pós-criação reaproveita o próprio formulário `NewInspectionForm` em modo edição (`inspectionId`/`initialData`/`initialEquipamentosPorCategoria`), nova rota `/inspections/[id]/editar`, nova RPC `update_inspection` (migration `00048`, espelha `create_inspection`, reconcilia equipamentos por id) e `updateInspectionAction` (grava auditoria quando é admin). Reconciliação de equipamento pede confirmação antes de remover um item já selecionado (evita perder foto por engano).

**Dois bugs reais de RLS encontrados e corrigidos no processo, mesma classe em duas tabelas irmãs:** `equipamento_inspecao` não tinha policy de UPDATE/DELETE (migration `00048`) e `equipamento_fotos` também não (achado só na revisão final whole-branch, migration `00049`) — sem elas, editar/remover equipamento ou trocar foto falhava silenciosamente sob RLS (RPC retornava sucesso, mas a linha não mudava). Outro achado real: o diálogo de confirmação de remoção, ao ser adicionado incondicionalmente em todo item de equipamento (Task 5), quebrou 2 testes num arquivo que a Task 5 nem tocava — só apareceu quando a Task 6 rodou a suíte completa (corrigido gating o `<dialog>` atrás de `initial`).

**Limpeza identificada, não aplicada ainda:** `parseEquipamentos`/`isEquipamentoValido`/`buildPhotoPath` (~35 linhas) estão duplicadas quase verbatim entre `app/(app)/inspections/new/actions.ts` (criação) e `app/(app)/inspections/[id]/editar/actions.ts` (edição, novo nesta rodada) — achado pelo `/ponytail:ponytail` depois do merge das tasks. Dois call sites reais agora, não é abstração prematura extrair pra um `lib/inspection/equipamento-form.ts` compartilhado. Não bloqueia, fica como próximo passo opcional.

**Ajustes pós-teste ao vivo (mesmo dia, depois de aplicar as migrations):** usuário confirmou que os 4 itens de "Documentação" (mesmo grupo `ordem=1`) também não deviam pontuar nem ficar no checklist — migration `00050`, mesmo padrão de `00047`. Isso esvaziou o grupo `ordem=1` por completo (Identificação+Histórico+Documentação eram seus 3 únicos subgrupos, 25 itens = 100% do grupo) — desativado via `ativo=false` (migration `00051`), mesmo tratamento já usado pro grupo 13 "Motoriz. Especial (F2)". O link "Editar dados básicos" mudou de lugar duas vezes a pedido do usuário: primeiro só na sidebar do checklist (Task 8), depois adicionado também na tela de resumo (usuário não achava vindo de "Ver"), por fim **movido pra dentro do conteúdo do primeiro grupo ativo** (card destacado antes dos itens, não mais um link na sidebar nem na tela de resumo).

**Bug real encontrado ao vivo, corrigido:** na aba Equipamentos (a mais longa) do formulário de edição, rolar a tela até o fim podia disparar um toque acidental no botão "Guardar alterações", salvando a inspeção sem intenção e criando uma entrada de auditoria falsa. Modo edição agora pede confirmação antes de gravar (mesmo padrão de cancelar/devolver/remover equipamento) — criação continua sem confirmação. Detalhe técnico: o botão de confirmar usa `formRef.requestSubmit()` em vez de `type="submit"` dentro do `<dialog>`, porque React 19 rejeita como "unexpected" uma submissão nativa associada via atributo `form="id"` fora da árvore do form — `requestSubmit()` é o caminho que a própria mensagem de erro do React recomenda.

**Pendência pra decisão futura:** a confirmação de "Guardar alterações" aparece sempre, mesmo se nenhum campo foi de fato alterado (não há rastreamento de "sujo"/dirty-state). Usuário perguntou se isso é normal — confirmado que sim, é o comportamento pretendido (mesma proteção contra toque acidental, independente de ter mudado algo). Rastrear mudança real em todos os 39 campos + equipamentos pra só perguntar quando algo mudou de verdade é possível, mas é trabalho bem maior — fica em aberto, sem decisão de fazer.

285/285 testes, `tsc` limpo. Cinco migrations novas (`00047` a `00051`) já aplicadas manualmente pelo usuário no Supabase. Branch `worktree-remocao-duplicacao-identificacao-historico` implementada, revisada, testada ao vivo com correções e mesclada em `main`.

## Fase 6 — Relatório final ✅ completo e mesclado em `main` (2026-08-14)

RF-43 a RF-53, RNF-13. Desenhado via `brainstorming` → `writing-plans` → `subagent-driven-development` a partir de um mockup visual de referência trazido pelo usuário — decisão explícita de exceção ao `DESIGN.md` (modo claro): a página do relatório usa identidade visual própria (dark glassmorphism, verde-menta `#00f5a0`, DM Sans, Material Symbols), escopada inteiramente a `.relatorio-page` via CSS custom properties, sem reconciliar com o resto do app. Design: `docs/superpowers/specs/2026-08-12-relatorio-final-design.md`, plano: `docs/superpowers/plans/2026-08-12-relatorio-final.md`.

**Implementação inicial (9 tasks via `subagent-driven-development`):** migration `00052` (colunas `parceiro_nome`/`parceiro_logo_url`/`parceiro_telefone` em `inspections`, recriação da view `inspections_with_flags` com `security_invoker=true`), diálogo "Fotos & Parceiro" no admin, `gerarCodigoCertificado()` (RF-51/RNF-13), `buildRelatorioGrupos` (agrupamento por severidade), página `/inspections/[id]/relatorio` com hero + especificações + `AnaliseTecnica`, filtros (Todos/Pontos de Atenção/Com Fotos), rodapé com técnico responsável (RF-53). RF-50 (dados do solicitante nunca aparecem no relatório) blindado por teste de regressão dedicado, reverificado por todos os revisores ao longo da sessão.

**Rodada extensa de teste ao vivo pelo usuário** (~20 commits de correção adicionais além das 9 tasks originais), guiada por HTML renderizado colado direto do navegador a cada rodada: redesenho completo da barra do dashboard (3 tentativas até alinhar largura/centralização com o resto das seções), carrossel de fotos automático (6s) + manual com lightbox, dados do parceiro + data da inspeção exibidos no dashboard, código de certificado exibido no hero e no card de veredito com botão de informação (modal explicativo + efeito "pisca até o primeiro clique"), contagem de itens por severidade nos cabeçalhos dos grupos de Análise Técnica (3 níveis — ok/médio/ruim — em vez de 2), duas seções novas com dados que já eram coletados mas nunca apareciam no relatório — "Histórico do veículo" (com bloco condicional "Veículo importado") e "Outros equipamentos" (`equipamento_inspecao`/`equipamento_fotos`, tabelas que o relatório nunca tinha consultado) —, glow esmeralda nos cards de especificações/análise técnica, correção de responsividade mobile (overflow horizontal real, não só visual).

**15 bugs reais encontrados e corrigidos ao longo da sessão**, os mais relevantes: drift de schema (`vehicle_data.situacao_fiscal_regular` virou texto livre e `situacao_fiscal_observacoes` foi removida pela migration `00040`, invalidando a primeira versão do bloco "Histórico" que tinha sido escrita contra o schema antigo — lembrete de sempre conferir contra a migration mais recente, não a primeira encontrada); overflow horizontal mobile causado por `flex: 1` sem `min-width: 0` num nome de item longo; `<dialog>` aninhado dentro de `<p>` (HTML inválido, warning de hidratação) — ocorreu duas vezes, corrigido nas duas; fotos de capa não recarregavam ao reabrir o diálogo "Fotos & Parceiro" do admin (`initialFotos={[]}` fixo, agora vem da query real).

**Revisão final whole-branch (2026-08-13, modelo mais capaz)** encontrou 0 Críticos e 6 Importantes, todos corrigidos numa única rodada de fix + re-revisão limpa: impressão descartava silenciosamente a seção "Outros equipamentos" (faltava o hook de forçar `<details>` aberto antes de imprimir — extraído pra `use-print-expands-details.ts`, compartilhado com `AnaliseTecnica`); a folha de impressão não escurecia `--relatorio-mint`, deixando código de certificado/badges quase invisíveis no papel; as 8 queries de checklist/equipamento ignoravam erro do Supabase, arriscando um relatório "certificado" silenciosamente vazio numa falha transitória — agora lançam (queries de `score`/fotos de capa continuam graciosas, por design); o anel do gauge nunca refletia a classificação (atributo `data-classificacao` morto) — corrigido, escopado só ao anel pra preservar o texto mint "Classe X" por pedido explícito anterior do usuário; `gerarCodigoCertificado()` usava `Math.random()`, contradizendo RNF-13 (código não-adivinhável) — trocado por `crypto.randomInt`.

**`ponytail:ponytail-review`** achou 5 pontos de simplicidade (~165 linhas cortáveis) — deferidos, não aplicados, mesmo critério de precedente já usado noutras fases deste roadmap.

329/329 testes, `tsc --noEmit` limpo, `npm run build` limpo. Mesclada em `main` via `finishing-a-development-branch` (merge local) em 2026-08-14 — único conflito real foi um add/add em `docs/superpowers/plans/2026-08-12-relatorio-final.md` (main tinha a versão original do plano; a branch tinha a versão corrigida, com o pré-preenchimento de `parceiro_logo_url`/`parceiro_telefone` no diálogo do admin pra evitar perda de dado ao salvar — resolvido tomando a versão da branch inteira). Worktree e branch `worktree-relatorio-final` removidos após confirmar testes/tipos limpos no `main` mesclado. Migration `00052` ainda precisa ser aplicada manualmente no Supabase, como as outras. `main` está à frente de `origin/main` (ainda não enviado ao remoto).

**Fora de escopo, não resolvido:** RF-52 (tela pública de consulta do certificado por código) segue fora do v1.0 — mas o texto do modal de `CertificadoInfoButton` já referencia `checkauto.pt` como se essa consulta existisse hoje. Sinalizado ao usuário; sem decisão de mudar a cópia por enquanto.

## Fase 7 — Acesso do cliente

RF-54 a RF-56. Tabela `client_access_logs` existe mas **sem nenhuma RLS policy** ainda.

**Prompt:**
> Vamos desenhar o acesso do cliente (RF-54 a RF-56), incluindo as RLS policies de `client_access_logs` que ainda não existem. Use `superpowers:brainstorming` e considere `docs/superpowers/plans/2026-07-10-inspecta-rls-policies.md` como referência de como desenhamos RLS antes.

## Fase 8 — Hardening & QA

RNFs restantes (performance, segurança, edge cases).

**Prompt:**
> Vamos levantar o escopo de hardening final (RNFs restantes). Use `superpowers:brainstorming` pra mapear o escopo; qualquer bug que aparecer no meio do processo usa `superpowers:systematic-debugging` antes de qualquer fix.

## Fase 9 — Motorização especial (futura)

Backlog, condicionada à compra de hardware (BEV/HEV/GPL). Conteúdo já está semeado no banco (grupo 12, `ativo=false`). **Não iniciar** sem sinal explícito do negócio — fora do roadmap de v1.0.

---

## Regra de transição entre passos (o que mantém o processo seguro)

Ao final de cada fase, depois que `subagent-driven-development` terminar todas as tasks:

1. `superpowers:requesting-code-review` no branch inteiro — sempre.
2. `ponytail:ponytail-review` — só se a fase adicionou código/abstração nova (pula em passos de housekeeping/docs).
3. `security-review` — só se a fase tocar auth, RLS ou controle de acesso (ex.: Fase 5 admin, Fase 7 acesso do cliente).
4. `verify` — só se a fase tiver UI (dirige a feature de ponta a ponta no navegador, não só testes/typecheck).
5. `superpowers:verification-before-completion` antes de qualquer alegação de "pronto" — sempre.
6. `superpowers:finishing-a-development-branch` pra decidir merge/PR — sempre.

Só depois disso cole o prompt da próxima fase da lista.

**Decisão registrada (2026-07-19):** avaliamos incluir `graphify` e `claude-mem` no gate e descartamos — `claude-mem` já observa passivamente via hooks, não precisa virar passo explícito; `graphify-out/` está parado desde 11/07 e revivê-lo como manutenção obrigatória seria YAGNI. `ponytail-audit`/`ponytail-debt` também ficaram de fora do gate por fase — cadência certa pra eles é periódica (ex.: uma vez antes da Fase 8/hardening), não repetida a cada fase.
